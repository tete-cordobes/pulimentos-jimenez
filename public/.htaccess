<IfModule mod_rewrite.c>
  RewriteEngine On
  # Una sola URL canónica: barra final. Sin esto GSC indexa /ruta y /ruta/.
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_URI} !/$
  RewriteRule ^(.*)$ /$1/ [R=301,L]
</IfModule>
